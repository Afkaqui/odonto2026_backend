# Copia este archivo a .env y completa los valores reales.
# NUNCA subas .env a git.

PORT=3000

# Conexion interna a postgres_db (red Docker postgresql_default).
# La BD odonto2026_db se crea en el VPS (ver README, paso de despliegue).
DATABASE_URL=postgresql://admin:K4qu1_Pr0d_2026@postgres_db:5432/odonto2026_db

# Clave que la app Flutter debe enviar en el header X-API-Key.
# Genera una con:  openssl rand -hex 24
API_KEY=cambia-esto-por-una-clave-larga-y-aleatoria
